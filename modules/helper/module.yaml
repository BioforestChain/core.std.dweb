product:
    type: lib
    platforms: [iosArm64,iosSimulatorArm64,iosX64,android,jvm]

dependencies:
    - $libs.kotlinx.datetime
    - $libs.ktor.http
    - $libs.ktor.io
    - $libs.squareup.okio


settings:
