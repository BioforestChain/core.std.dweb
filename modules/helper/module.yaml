product:
    type: lib
    platforms: [iosArm64,iosSimulatorArm64,iosX64,android,jvm]

dependencies:
    - $libs.kotlinx.datetime
    - $libs.ktor.http
    - $libs.ktor.io
    - $libs.squareup.okio
    - $libs.dweb.rust.libs.compression

aliases:
    - desktopAndAndroid: [jvm, android]

dependencies@desktopAndAndroid:
    - $libs.apache.common.compress

dependencies@ios:
    - $libs.squareup.okio



